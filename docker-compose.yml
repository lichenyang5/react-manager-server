services:
  mongo:
    image: mongo:7
    container_name: react-manager-mongo
    ports:
      - "27018:27017"
    volumes:
      - mongo-data:/data/db
    restart: unless-stopped

volumes:
  mongo-data:
